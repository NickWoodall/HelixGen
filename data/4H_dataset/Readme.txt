Only the models folder is required to retrain the GAN.
str_models is required with models to retrain the GraphGen network
LoopToFit is only used for replicating fitting the loop structures for the Looping methods.

Avaible from download from google drive.